{# templates/circuito/nuevo.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Nuevo Circuito{% endblock %}

{% block body %}
    <h1>Crear Circuito</h1>

    {{ form_start(form) }}
        {{ form_row(form.id) }}
        {{ form_row(form.longitud) }}
        {{ form_row(form.curvas) }}
        {{ form_row(form.vueltaRecord) }}
        {{ form_row(form.inauguracion) }}
        {{ form_row(form.historia) }}
        <button class="btn btn-primary" type="submit">Crear Circuito</button>
    {{ form_end(form) }}
{% endblock %}
